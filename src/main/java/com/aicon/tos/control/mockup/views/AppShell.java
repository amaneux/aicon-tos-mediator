package com.aicon.tos.control.mockup.views;

/**
 * The `AppShell` class serves as a configuration entry point for the Vaadin application.
 * It implements the `AppShellConfigurator` interface, which allows customization of the application shell.
 * <p>
 * This class uses the `@Push` annotation to enable server push functionality,
 * allowing the server to send updates to the client without a client request.
 */
//@Push
//public class AppShell implements AppShellConfigurator {
//    // This class is currently used to enable server push through the @Push annotation
//    // and does not require any additional implementation.
//}
